# Dig tunnels to work from local machine
ssh -L 1234:localhost:8786 -L 8080:localhost:8080 vm_master

# Reboot all the vms (useful to hard shut down the cluster if needed, even though a better way may exist)
for i in vm_master vm_minion{1..3}; do ssh $i 'sudo reboot'; done
